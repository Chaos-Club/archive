{
  "name": "01-typescript",
  "version": "0.0.0",
  "description": "Tim's first project",
  "main": "index.ts",
  "repository": "https://github.com/iefomit/01-typescript",
  "author": "Timofei",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild chaos-game/index.ts l-systems_koch/index.ts l-systems_sierpinski/index.ts --bundle --outdir=. --outbase=. --entry-names=[dir]/bundle --watch --servedir=.",
    "build": "esbuild chaos-game/index.ts l-systems_koch/index.ts l-systems_sierpinski/index.ts --bundle --outdir=. --outbase=. --entry-names=[dir]/bundle --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^5.30.5",
    "@typescript-eslint/parser": "^5.30.5",
    "eslint": "^8.19.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-prettier": "^8.5.0",
    "nodemon": "^2.0.19",
    "readline-sync": "^1.4.10",
    "ts-node": "^10.8.2",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/node": "^18.0.4",
    "esbuild": "^0.27.3"
  }
}
